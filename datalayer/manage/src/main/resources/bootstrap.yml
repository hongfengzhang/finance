spring:
  application:
    name: manage
  profiles:
    active: @active@
  cloud:
    config:
      enabled: true
      profile: @active@
      name: ${spring.application.name}
      uri: @config.server@