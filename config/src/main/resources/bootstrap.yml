spring:
  application:
    name: config
  profiles:
    active: @active@
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yuyidi/config-repo
      discovery:
        enabled: true
#security:
#  basic:
#    enabled: true
#  user:
#    name: wabenkeji
#    password: waben0605