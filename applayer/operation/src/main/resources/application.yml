server:
  port: 8082
  context-path: /operation
spring:
  mvc:
    throw-exception-if-no-handler-found: true
  thymeleaf:
    cache: false
hystrix:
  command:
    default:
     execution:
        isolation:
          thread:
            timeoutInMilliseconds: 10000
        timeout:
            enabled: false