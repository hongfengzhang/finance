server:
  port: 8082
  context-path: /operation
spring:
  mvc:
    throw-exception-if-no-handler-found: true
  thymeleaf:
    cache: false
feign:
  command:
    default:
      execution:
        timeout:
          enabled: false