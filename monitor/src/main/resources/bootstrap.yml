spring:
  application:
    name: monitor
  profiles:
    active: @active@
  cloud:
    config:
      enabled: true
      profile: @active@
      name: ${spring.application.name}
      uri: @config.server@
turbine:
  app-config: tactics
  aggregator:
    cluster-config: default
  cluster-name-expression:  new String("default")