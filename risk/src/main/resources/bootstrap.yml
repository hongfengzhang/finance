spring:
  application:
    name: risk
  profiles:
    active: @active@
  cloud:
    config:
      enabled: true
      profile: @active@
      name: ${spring.application.name}
      uri: @config.server@